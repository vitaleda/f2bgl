void main(
	float3 aPosition,
	uniform float psize,
	column_major uniform float4x4 wvp,
	float out vPsize : PSIZE,
	float4 out vPosition: POSITION)
{
	vPosition = mul(float4(aPosition, 1.f), wvp);

	vPsize = psize;
}

